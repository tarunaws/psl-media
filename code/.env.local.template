# Local environment variables for MediaGenAI services
# Copy this file to `.env.local` (kept out of version control) and populate with your real values.

# AWS credentials
AWS_ACCESS_KEY_ID=REPLACE_WITH_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=REPLACE_WITH_SECRET_ACCESS_KEY
AWS_REGION=us-east-1

# Personalized trailer service
PERSONALIZED_TRAILER_PIPELINE_MODE=aws
PERSONALIZED_TRAILER_REGION=us-east-1
PERSONALIZED_TRAILER_S3_BUCKET=REPLACE_WITH_BUCKET_NAME

# Optional: override for other services
# MEDIA_SUPPLY_CHAIN_BUCKET=REPLACE_WITH_BUCKET_NAME
# DYNAMO_TABLE=REPLACE_WITH_TABLE_NAME
