# Build-time backend endpoints for the AWS deployment frontend.
# Replace each value with the CloudFront default domain for that service.

# AI Subtitle service CloudFront URL (example: https://dxxxxx.cloudfront.net)
REACT_APP_SUBTITLE_API_BASE=https://REPLACE_WITH_AI_SUBTITLE_CLOUDFRONT_DOMAIN

# Highlight & Trailer service CloudFront URL
REACT_APP_HIGHLIGHT_TRAILER_API=https://REPLACE_WITH_HIGHLIGHT_TRAILER_CLOUDFRONT_DOMAIN

# Dynamic Ad Insertion service CloudFront URL
REACT_APP_DAI_BACKEND_URL=https://REPLACE_WITH_DAI_CLOUDFRONT_DOMAIN
