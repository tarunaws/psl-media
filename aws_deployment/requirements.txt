## Shared (workspace-wide) Python requirements
##
## Install into the single shared venv at `code/.venv/`.
## This file is the baseline dependency set for running the Flask services.
## Optional/ML-heavy features (YOLO, InsightFace) live in `requirements-optional-ml.txt`.

# Core
python-dotenv==1.0.1
requests==2.32.5
sentry-sdk==1.45.0

# Web
Flask==3.0.3
Flask-Cors==4.0.0
Flask-Limiter==3.5.0

# AWS
boto3==1.42.16

# Media / documents
pillow==11.3.0
PyPDF2>=3.0.0
pypdf>=4.0.0
python-docx>=1.1.0

# Misc utilities
Unidecode==1.3.8

# Optional / feature-specific deps (some workflows may not be used in every run)
markdown>=3.6
reportlab>=4.0
weasyprint>=61.0
pytube>=15.0.0
